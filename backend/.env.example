# Copy to .env and fill in to enable the live LLM agent.
# If left empty, the system automatically falls back to the rule-based agent
# so the demo still works fully offline.

# Reasoning layer. Deck lists "GPT-4o or Llama 3"; this build defaults to
# Anthropic Claude and is provider-swappable. Set ONE of the following.
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=            # optional alternate provider (not required)

# Model used for reasoning / routing / drafting
LLM_MODEL=claude-haiku-4-5-20251001
